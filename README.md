# FunnelIQ
### Marketing Attribution & Funnel ROI Dashboard

Run:
```bash
pip install -r requirements.txt
streamlit run app.py
```

Compares last-touch and linear attribution on synthetic multi-touch journeys. The dashboard shows funnel conversion, channel ROI/CAC, an illustrative reallocation simulation, and Excel export. Attribution is illustrative, not causal.